diff --git a/node_modules/expo-splash-screen/plugin/build/withSplashScreen.js b/node_modules/expo-splash-screen/plugin/build/withSplashScreen.js
index 40a944f..04fd5d7 100644
--- a/node_modules/expo-splash-screen/plugin/build/withSplashScreen.js
+++ b/node_modules/expo-splash-screen/plugin/build/withSplashScreen.js
@@ -5,14 +5,14 @@ const withIosSplashScreen_1 = require("@expo/prebuild-config/build/plugins/unver
 const config_plugins_1 = require("expo/config-plugins");
 const pkg = require('expo-splash-screen/package.json');
 const withSplashScreen = (config, props) => {
-    let android = null;
-    let ios = null;
+    let android;
+    let ios;
     const resizeMode = props?.resizeMode || 'contain';
     const { ios: iosProps, android: androidProps, ...otherProps } = props ?? {};
     const usesLegacySplashConfigIOS = !props || (androidProps && !iosProps && Object.keys(otherProps).length === 0);
     const usesLegacySplashConfigAndroid = !props || (iosProps && !androidProps && Object.keys(otherProps).length === 0);
     android = usesLegacySplashConfigAndroid
-        ? null
+        ? undefined
         : {
             ...otherProps,
             ...androidProps,
@@ -23,7 +23,7 @@ const withSplashScreen = (config, props) => {
             },
         };
     ios = usesLegacySplashConfigIOS
-        ? null
+        ? undefined
         : {
             ...otherProps,
             ...iosProps,
@@ -33,7 +33,7 @@ const withSplashScreen = (config, props) => {
                 ...iosProps?.dark,
             },
         };
-    // Passing null here will result in the legacy splash config being used.
+    // Legacy path must pass `undefined` (not null) so Android `withAndroidSplashScreen` sets isLegacyConfig.
     config = (0, withAndroidSplashScreen_1.withAndroidSplashScreen)(config, android);
     config = (0, withIosSplashScreen_1.withIosSplashScreen)(config, ios);
     return config;
diff --git a/node_modules/expo-splash-screen/plugin/src/withSplashScreen.ts b/node_modules/expo-splash-screen/plugin/src/withSplashScreen.ts
index 131af90..e0ecdac 100644
--- a/node_modules/expo-splash-screen/plugin/src/withSplashScreen.ts
+++ b/node_modules/expo-splash-screen/plugin/src/withSplashScreen.ts
@@ -21,8 +21,8 @@ type PluginConfig = {
 };
 
 const withSplashScreen: ConfigPlugin<PluginConfig | null> = (config, props) => {
-  let android: AndroidSplashConfig | null = null;
-  let ios: IOSSplashConfig | null = null;
+  let android: AndroidSplashConfig | undefined;
+  let ios: IOSSplashConfig | undefined;
 
   const resizeMode = props?.resizeMode || 'contain';
 
@@ -34,7 +34,7 @@ const withSplashScreen: ConfigPlugin<PluginConfig | null> = (config, props) => {
     !props || (iosProps && !androidProps && Object.keys(otherProps).length === 0);
 
   android = usesLegacySplashConfigAndroid
-    ? null
+    ? undefined
     : {
         ...otherProps,
         ...androidProps,
@@ -46,7 +46,7 @@ const withSplashScreen: ConfigPlugin<PluginConfig | null> = (config, props) => {
       };
 
   ios = usesLegacySplashConfigIOS
-    ? null
+    ? undefined
     : {
         ...otherProps,
         ...iosProps,
@@ -57,7 +57,7 @@ const withSplashScreen: ConfigPlugin<PluginConfig | null> = (config, props) => {
         },
       };
 
-  // Passing null here will result in the legacy splash config being used.
+  // Legacy path must pass `undefined` (not null) so Android `withAndroidSplashScreen` sets isLegacyConfig.
   config = withAndroidSplashScreen(config, android);
   config = withIosSplashScreen(config, ios);
   return config;
